{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start React",
      "type": "shell",
      "command": "cd frontend && BROWSER=none NODE_OPTIONS=--openssl-legacy-provider npm start",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Starting the development server",
          "endsPattern": "Compiled successfully|Failed to compile"
        }
      }
    }
  ]
}
